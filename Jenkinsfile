pipeline{agent any 
         stages{ stage('Clone Repo') 
          { steps 
                  {  
                   sh "sudo su",
                   sh"touch index1.html"}
           }
         }
        
        }
