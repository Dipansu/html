pipeline{agent any 
         stages{ stage('Clone Repo') 
          { steps 
                  { 
                   sh "scp index,html ec2-user@localhost:test"}
           }
         }
        
        }
