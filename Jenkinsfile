pipeline{agent any 
         stages{ stage('Clone Repo') 
          { steps 
                  { 
                   sh "scp file://index.html localhost:index.html"}
           }
         }
        
        }
